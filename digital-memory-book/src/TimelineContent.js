import React from 'react';

const TimelineContent = ({ name }) => {
  return (
    <div>
      {/* Your timeline-related content here */}
      <p>This is the timeline content for {name}</p>
    </div>
  );
};

export default TimelineContent;